export enum ApiEndpoints {
    users = '/api/users',
    notes = '/api/notes'
}

export enum ExtendedEndpoints {
    login = '/login',
    update = '/update',
    delete = '/delete',
    assign = '/assign'
}